{
  "name": "chatterbox2",
  "version": "0.0.0",
  "scripts": {
    "build": "tsc && npm run build:copy-assets",
    "build:copy-assets": "copyfiles src/assets/* src/assets/**/* --up 2 --exclude **/*.ts dist/assets/",
    "format": "prettier --write .",
    "lint": "prettier --check .",
    "test": "jest",
    "prestart": "npm run lint && npm run build",
    "start": "twilio-run --functions-folder dist/functions --assets-folder dist/assets",
    "predeploy": "npm run lint && npm run build",
    "deploy": "twilio-run deploy --functions-folder dist/functions --assets-folder dist/assets --runtime node22 --override-existing-project",
    "promote": "twilio-run promote --to= --from=dev"
  },
  "dependencies": {
    "@twilio-labs/serverless-runtime-types": "^4.0.1"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^25.6.0",
    "copyfiles": "^2.4.1",
    "jest": "^30.3.0",
    "prettier": "^3.8.1",
    "ts-jest": "^29.4.9",
    "twilio-run": "^5.0.1",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": "22"
  }
}
